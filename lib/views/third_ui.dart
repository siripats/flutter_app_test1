
import 'package:flutter/material.dart';

class ThidUI extends StatelessWidget {
  const ThidUI({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
class _ThidUIState extends StatelessWidget {
  const _ThidUIState({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}
  }
}