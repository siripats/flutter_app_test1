import 'package:flutter/material.dart';

class ForthUI extends StatelessWidget {
  const ForthUI({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
